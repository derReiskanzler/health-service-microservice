export type ServiceName = 'INDEX' | 'STORAGE' | 'CRAWLER' | 'CLIENT' | 'FRONTEND' | 'USER' | 'HEALTH';

export const ServiceName = {
    INDEX: 'INDEX' as ServiceName,
    STORAGE: 'STORAGE' as ServiceName,
    CRAWLER: 'CRAWLER' as ServiceName,
    CLIENT: 'CLIENT' as ServiceName,
    FRONTEND: 'FRONTEND' as ServiceName,
    USER: 'USER' as ServiceName,
    HEALTH: 'HEALTH' as ServiceName
};

